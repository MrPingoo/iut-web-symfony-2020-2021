<header>
    <div class="collapse bg-dark" id="navbarHeader">
        <div class="container">
            <div class="row">
                <div class="col-sm-8 col-md-7 py-4">
                    <h4 class="text-white"></h4>
                    <p class="text-muted"></p>
                </div>
                <div class="col-sm-4 offset-md-1 py-4">
                    <h4 class="text-white">Menu</h4>
                    <ul class="list-unstyled">

                        {% if app.user %}
                            <li><a href="{{ path('app_logout') }}" class="text-white">Logout</a></li>
                            <li><a href="{{ path('front_commands') }}" class="text-white">Mes commandes</a></li>
                        {% else %}
                            <li><a href="{{ path('app_register') }}" class="text-white">Créer un compte</a></li>
                            <li><a href="{{ path('app_login') }}" class="text-white">Se connecter</a></li>
                        {% endif %}

                        {% if is_granted('ROLE_ADMIN') %}
                            <li><a href="{{ path('brand_index') }}" class="text-white">Marques</a></li>
                            <li><a href="{{ path('category_index') }}" class="text-white">Catégorie</a></li>
                            <li><a href="{{ path('sub_category_index') }}" class="text-white">Sous-catégorie</a></li>
                            <li><a href="{{ path('user_index') }}" class="text-white">Utilisateurs</a></li>
                            <li><a href="{{ path('product_index') }}" class="text-white">Produits</a></li>
                        {% else %}

                        {% endif %}
                    </ul>
                </div>
            </div>
        </div>
    </div>
    <div class="navbar navbar-dark bg-dark shadow-sm">
        <div class="container d-flex justify-content-between">
            <a href="#" class="navbar-brand d-flex align-items-center">
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" aria-hidden="true" class="mr-2" viewBox="0 0 24 24" focusable="false"><path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"/><circle cx="12" cy="13" r="4"/></svg>
                <strong>E-commerce</strong>
            </a>
            <div>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarHeader" aria-controls="navbarHeader" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                {% if app.user %}
                    <a class="btn btn-success btn-sm ml-2 cart-btn" href="#">
                        <i class="fa fa-shopping-cart"></i> Panier
                        <!--<span class="badge badge-light">3</span>-->
                    </a>
                {% endif %}
            </div>
        </div>
    </div>
</header>