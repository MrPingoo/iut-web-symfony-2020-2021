{% extends 'base.html.twig' %}

{% block title %}SubCategory{% endblock %}

{% block body %}
    <h1>SubCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sub_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sub_category.name }}</td>
            </tr>
            <tr>
                <th>DeletedAt</th>
                <td>{{ sub_category.deletedAt ? sub_category.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sub_category_index') }}">back to list</a>

    <a href="{{ path('sub_category_edit', {'id': sub_category.id}) }}">edit</a>

    {{ include('sub_category/_delete_form.html.twig') }}
{% endblock %}
